apiVersion: v1
kind: Service
metadata:
  name: task-api-dev
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
  selector:
    app: task-api
    env: development
